# Lightweight requirements for the deployed API server (Render)
# These packages are sufficient to serve all GET endpoints from pre-computed CSVs.
# Heavy packages (sentence-transformers, faiss, torch) are NOT needed here —
# they are only required for local pipeline execution (rank.py / rank_advanced.py).

numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
pyyaml>=6.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.7
